import 'package:flutter/material.dart';
import './XDComponent621.dart';

class XDSignUp extends StatelessWidget {
  XDSignUp({
    Key key,
  }) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: const Color(0xffffffff),
      body: Stack(
        children: <Widget>[
          SizedBox(
            width: 375.0,
            height: 1178.0,
            child: XDComponent621(),
          ),
        ],
      ),
    );
  }
}
